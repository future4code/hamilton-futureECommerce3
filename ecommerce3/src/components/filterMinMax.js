import React from "react";
import styled from "styled-components";
import PropTypes from 'prop-types';

const filterMinMax = styled.div`

`;
export class filterMinMax extends React.Component {
    constructor(props) {
        super(props);
        
    }

    render() {
        return (
            <filterMinMax>

            </filterMinMax>
        );
    }
}

export default filterMinMax;

